// // в файлах с названием loading указываем как будет выглядеть loader при ожидании загрузки
import React from "react";

const Loading = () => {
  return <div>Loading</div>;
};

export default Loading;
